@using Microsoft.AspNetCore.Mvc.TagHelpers
@model Stream.ViewModels.ViewModels.LibraryViewModel

@{
    ViewData["Title"] = "Usuń grę z biblioteki";
}

<div class="container mt-4">
    <h2 class="mb-4">Usuń grę z biblioteki</h2>

    <h4>Czy na pewno chcesz usunąć tę grę z biblioteki?</h4>

    <div class="card mb-4">
        <div class="card-header">
            Informacje o bibliotece
        </div>
        <div class="card-body">
            <p><strong>Tytuł gry:</strong> @Model.Libraries.FirstOrDefault()?.Game?.Title</p>
            <p><strong>Użytkownik:</strong> @Model.Libraries.FirstOrDefault()?.User?.Username</p>
            <p><strong>Status:</strong> @Model.Libraries.FirstOrDefault()?.Status</p>
        </div>
    </div>

    <div class="d-flex justify-content-between">
        <form asp-action="Delete" method="post">
            <input type="hidden" value="@Model.Libraries.FirstOrDefault()?.Id" name="Id" />
            <button type="submit" class="btn btn-danger btn-lg">Usuń</button>
        </form>
        <a href="@Url.Action("Index")" class="btn btn-secondary btn-lg">Anuluj</a>
    </div>
</div>